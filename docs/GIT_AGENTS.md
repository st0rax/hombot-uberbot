# Git-Identitäten (HomBot, projektlokal)

Nicht `*@webagent.local` und nicht die Dummy-Tabelle kopieren.

| Agent | Git author |
| --- | --- |
| pflege | `pflege <pflege@hombot.local>` |
| derfuhrer | `derfuhrer <derfuhrer@hombot.local>` |
| koordinator | `koordinator <koordinator@hombot.local>` |
| skeptiker | `skeptiker <skeptiker@hombot.local>` |
| rust | `rust <rust@hombot.local>` |
| designer | `designer <designer@hombot.local>` |
| dompteur | `dompteur <dompteur@hombot.local>` |
| bitfluesterer | `bitfluesterer <bitfluesterer@hombot.local>` |
| nachtschicht | `nachtschicht <nachtschicht@hombot.local>` |
| pseudo-mensch | `pseudo-mensch <pseudo-mensch@hombot.local>` |

Commits: `GIT_AUTHOR_NAME` / `GIT_AUTHOR_EMAIL` aus dieser Tabelle.
Windows darf ein lokales `commit-as-agent` bauen; Linux setzt die Env.
Nur `main` oder `feature|fix|docs|chore|refactor|test/<kurz>`.
